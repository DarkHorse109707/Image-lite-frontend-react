import { Template } from "@/components"

export default function FormularioPage() {
    return (
        <Template loading = {false}>

            <h1>
                Formulário
            </h1>
        </Template>
    )
}